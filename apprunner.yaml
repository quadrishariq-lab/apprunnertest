version: 1.0
runtime: python311

build:
  commands:
    - pip3 install flask 
    - pip3 install gunicorn
    
run:
  pre-run:
    - pip3 install gunicorn
    - pip3 install flask
  command: gunicorn -b 0.0.0.0:8080 app:app
